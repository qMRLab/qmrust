[package]
name = "qmrust-cli"
description = "qmrust command-line interface"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[[bin]]
name = "qmrust"
path = "src/main.rs"

[dependencies]
qmrust-core = { path = "../qmrust-core" }
rust-bids = { path = "../rust-bids" }
anyhow.workspace = true
ndarray.workspace = true
rayon.workspace = true
serde.workspace = true
serde_yaml.workspace = true
serde_json.workspace = true
clap.workspace = true
nifti.workspace = true
matfile.workspace = true
indicatif.workspace = true
owo-colors.workspace = true

[dev-dependencies]
# The in-memory BIDS resolver, so a test can assert it resolves a real dataset
# to exactly what this crate's on-disk path resolves it to.
qmrust-wasm = { path = "../qmrust-wasm" }
